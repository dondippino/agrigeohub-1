import { OnInit, OnChanges, EventEmitter, OnDestroy, ViewContainerRef, ComponentFactoryResolver, SimpleChanges } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { FormlyConfig } from '../services/formly.config';
import { FormlyFieldConfig, FormlyFormOptions } from './formly.field.config';
export declare class FormlyField implements OnInit, OnChanges, OnDestroy {
    private formlyConfig;
    private componentFactoryResolver;
    model: any;
    form: FormGroup;
    field: FormlyFieldConfig;
    options: FormlyFormOptions;
    modelChange: EventEmitter<any>;
    fieldComponent: ViewContainerRef;
    private componentRefs;
    constructor(formlyConfig: FormlyConfig, componentFactoryResolver: ComponentFactoryResolver);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private createFieldComponent();
    private getFieldWrappers(type);
    private mergeTemplateManipulators(source, target);
    private createComponent(fieldComponent, component);
}
