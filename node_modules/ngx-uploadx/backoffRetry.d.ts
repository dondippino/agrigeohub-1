/**
 *  Exponential Backoff Retries
 */
export declare class BackoffRetry {
    private minInterval;
    private maxInterval;
    private k;
    private delay;
    /**
     *
     * Creates an instance of BackoffRetry.
     * @default
     * minInterval = 1000 ms
     * maxInterval = minInterval * 120 (2 min)
     * k = 2
     */
    constructor(minInterval?: number, maxInterval?: number, k?: number);
    /**
     * Delay Retry
     */
    wait(): Promise<{}>;
    /**
     * Reset Retry Interval
     */
    reset(): void;
}
